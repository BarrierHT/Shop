<%- include('./includes/head.ejs') %>
    
    </head>
<body>

<%- include('./includes/navigation.ejs') %>

	<% if(users.length > 0){ %>
		<h1>Users: </h1>
		<ul>
			<% users.forEach( (element,index) => { %>
					<li> <h4> Username: <%= element['title'] %> At the index: <%= index %> </h4> </li>
			<% }); %>
		</ul>
	<% } else { %>
		<h1>There are no users!</h1>
	<% } %>

<%- include('./includes/end.ejs') %>
